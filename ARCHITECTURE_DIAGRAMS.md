# System Architecture Diagrams

## 📐 Visual Guide to Enterprise Church App Architecture

This document contains ASCII and text-based diagrams to help visualize the system architecture.

---

## 1. High-Level System Overview

```
┌────────────────────────────────────────────────────────────────────┐
│                          USERS                                     │
│  Church Members | Church Leaders | Church Admins | Super Admins   │
└────────────────────┬───────────────────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────────────────────┐
│                     CLIENT APPLICATIONS                            │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
│  │   Web App    │    │   iOS App    │    │ Android App  │       │
│  │   (Next.js   │    │ (Capacitor/  │    │ (Capacitor/  │       │
│  │    + PWA)    │    │   Native)    │    │   Native)    │       │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘       │
│         │                   │                    │               │
│         └───────────────────┴────────────────────┘               │
│                             │                                     │
└─────────────────────────────┼─────────────────────────────────────┘
                              │
                              │ HTTPS/WSS
                              │
┌─────────────────────────────┼─────────────────────────────────────┐
│                      API GATEWAY                                   │
├─────────────────────────────┼─────────────────────────────────────┤
│                             │                                     │
│                      ┌──────▼──────┐                             │
│                      │  Next.js    │                             │
│                      │ API Routes  │                             │
│                      │             │                             │
│                      │   + Auth    │                             │
│                      │ Middleware  │                             │
│                      └──────┬──────┘                             │
│                             │                                     │
└─────────────────────────────┼─────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
┌─────────▼────────┐ ┌────────▼────────┐ ┌───────▼────────┐
│   Supabase       │ │ External APIs   │ │  Redis Cache   │
│                  │ │                 │ │                │
│ • PostgreSQL     │ │ • YouTube API   │ │ • Session      │
│ • Authentication │ │ • Bible API     │ │ • Query Cache  │
│ • Storage        │ │ • Payment API   │ │ • Rate Limit   │
│ • Realtime       │ │ • SMS Provider  │ └────────────────┘
│ • Edge Functions │ └─────────────────┘
└──────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                       BACKGROUND SERVICES                          │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  Cron    │  │  Push    │  │  Email   │  │  Data    │         │
│  │  Jobs    │  │  Notif   │  │  Service │  │  Sync    │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                    MONITORING & OBSERVABILITY                      │
├────────────────────────────────────────────────────────────────────┤
│  Sentry (Errors) | Vercel Analytics | PostHog (Product Analytics) │
└────────────────────────────────────────────────────────────────────┘
```

---

## 2. Data Flow Architecture

```
USER ACTION (e.g., Create Announcement)
        │
        ▼
┌───────────────┐
│  React        │
│  Component    │
└───────┬───────┘
        │ form submit
        ▼
┌───────────────┐
│  API Client   │
│  (fetch/axios)│
└───────┬───────┘
        │ POST /api/announcements
        ▼
┌───────────────┐
│  Next.js      │
│  API Route    │
└───────┬───────┘
        │
        ├─► Authentication Check (Supabase)
        │
        ├─► Authorization Check (RLS)
        │
        ├─► Validation (Zod)
        │
        ▼
┌───────────────┐
│  Database     │
│  (PostgreSQL) │
└───────┬───────┘
        │ Insert success
        ▼
┌───────────────┐
│  Realtime     │
│  Broadcast    │  ──► Other clients get update
└───────┬───────┘
        │
        ▼
┌───────────────┐
│  Background   │
│  Jobs         │
└───────┬───────┘
        │
        ├─► Send push notifications
        ├─► Send email notifications
        └─► Log analytics event
```

---

## 3. Database Relationships

```
┌─────────────────┐
│    churches     │
│─────────────────│
│ id (PK)         │
│ name            │
│ slug            │
│ ...             │
└────────┬────────┘
         │
         │ 1:N
         │
┌────────▼────────┐
│     users       │
│─────────────────│
│ id (PK)         │
│ church_id (FK)  │───┐
│ email           │   │
│ full_name       │   │ User creates/owns multiple things
│ role            │   │
└─────────────────┘   │
                      │
         ┌────────────┼────────────┬─────────────┬──────────────┐
         │            │            │             │              │
         │ 1:N        │ 1:N        │ 1:N         │ 1:N          │ 1:N
         ▼            ▼            ▼             ▼              ▼
┌────────────┐ ┌─────────────┐ ┌──────────┐ ┌─────────┐ ┌──────────┐
│bible_      │ │announcements│ │  posts   │ │ prayers │ │ quizzes  │
│reading_    │ │─────────────│ │──────────│ │─────────│ │──────────│
│progress    │ │ id          │ │ id       │ │ id      │ │ id       │
│────────────│ │ church_id   │ │ church_id│ │ user_id │ │ church_id│
│ user_id(FK)│ │ author_id   │ │ author_id│ │ ...     │ │ ...      │
│ book_id    │ │ ...         │ │ ...      │ └─────────┘ └──────────┘
│ chapter    │ └─────────────┘ └──────────┘
└────────────┘

┌─────────────────┐
│ reading_plans   │
│─────────────────│
│ id (PK)         │
│ church_id (FK)  │
│ ...             │
└────────┬────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐
│user_reading_    │
│plans            │
│─────────────────│
│ user_id (FK)    │
│ plan_id (FK)    │
│ start_date      │
│ current_day     │
└─────────────────┘
```

---

## 4. Authentication Flow

```
┌──────────────┐
│   User       │
│   (Browser)  │
└──────┬───────┘
       │
       │ 1. Visit /login
       ▼
┌──────────────┐
│  Login Page  │
│  (Next.js)   │
└──────┬───────┘
       │
       │ 2. Submit email + password
       ▼
┌──────────────┐
│  Supabase    │
│  Auth API    │
└──────┬───────┘
       │
       │ 3. Verify credentials
       ├─► Create session token
       │
       ▼
┌──────────────┐
│  Set Cookie  │
│  (httpOnly)  │
└──────┬───────┘
       │
       │ 4. Redirect to /dashboard
       ▼
┌──────────────┐
│  Middleware  │
│  (Check Auth)│
└──────┬───────┘
       │
       │ 5. Verify token
       ├─► Get user from Supabase
       │
       ▼
┌──────────────┐
│  Dashboard   │
│  (Protected) │
└──────────────┘

Subsequent Requests:
┌──────────────┐
│  User Action │
└──────┬───────┘
       │
       │ Include cookie with request
       ▼
┌──────────────┐
│  API Route   │
└──────┬───────┘
       │
       │ Extract token from cookie
       ▼
┌──────────────┐
│  Supabase    │
│  getUser()   │
└──────┬───────┘
       │
       │ Return user data
       ▼
┌──────────────┐
│  Authorize   │
│  Action      │
└──────────────┘
```

---

## 5. Feature Module Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    FEATURE MODULES                          │
└─────────────────────────────────────────────────────────────┘

Each feature module follows this structure:

app/
└── (dashboard)/
    └── dashboard/
        └── [feature-name]/
            ├── page.tsx           (Main page)
            ├── [id]/
            │   └── page.tsx       (Detail page)
            └── components/        (Feature components)
                ├── FeatureList.tsx
                ├── FeatureCard.tsx
                ├── FeatureForm.tsx
                └── FeatureModal.tsx

components/
└── [feature-name]/
    ├── index.ts
    ├── FeatureComponent.tsx
    └── ...

lib/
├── api/
│   └── [feature-name].ts      (API functions)
├── hooks/
│   └── use[Feature].ts        (Custom hooks)
└── validators/
    └── [feature-name].ts      (Zod schemas)

app/api/
└── [feature-name]/
    ├── route.ts               (GET, POST list)
    └── [id]/
        └── route.ts           (GET, PUT, DELETE single)

Example: Announcements Module
────────────────────────────────
app/
└── (dashboard)/
    └── dashboard/
        └── announcements/
            ├── page.tsx                    [List View]
            ├── new/
            │   └── page.tsx                [Create Form]
            └── [id]/
                ├── page.tsx                [Detail View]
                └── edit/
                    └── page.tsx            [Edit Form]

components/
└── announcements/
    ├── AnnouncementCard.tsx
    ├── AnnouncementList.tsx
    ├── AnnouncementForm.tsx
    ├── AnnouncementFilters.tsx
    └── index.ts

lib/
├── api/
│   └── announcements.ts
│       ├── fetchAnnouncements()
│       ├── createAnnouncement()
│       ├── updateAnnouncement()
│       └── deleteAnnouncement()
├── hooks/
│   └── useAnnouncements.ts
│       └── useAnnouncements() hook
└── validators/
    └── announcement.ts
        └── announcementSchema (Zod)

app/api/
└── announcements/
    ├── route.ts
    │   ├── GET  /api/announcements
    │   └── POST /api/announcements
    └── [id]/
        └── route.ts
            ├── GET    /api/announcements/:id
            ├── PUT    /api/announcements/:id
            └── DELETE /api/announcements/:id
```

---

## 6. State Management Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    STATE LAYERS                             │
└─────────────────────────────────────────────────────────────┘

1. Server State (React Query)
   ├─ API data fetching
   ├─ Caching
   ├─ Background refetching
   └─ Optimistic updates

2. Client State (Zustand)
   ├─ User preferences (theme, language)
   ├─ UI state (modals, sidebars)
   ├─ Temporary data
   └─ Application settings

3. URL State (Next.js Router)
   ├─ Current page
   ├─ Query parameters
   ├─ Filters & sorting
   └─ Pagination

4. Form State (React Hook Form)
   ├─ Input values
   ├─ Validation errors
   ├─ Touched fields
   └─ Submission status

5. Local Storage
   ├─ Auth token (httpOnly cookie)
   ├─ User preferences (persisted)
   ├─ Draft content
   └─ Offline queue


Data Flow Example:
──────────────────

User clicks "Load Announcements"
        │
        ▼
┌───────────────────┐
│ React Query       │
│ useQuery()        │
└─────────┬─────────┘
          │
          ├─► Check cache
          │   ├─ Hit: Return cached data
          │   └─ Miss: Fetch from API
          │
          ▼
┌───────────────────┐
│ API Route         │
│ /api/announcements│
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ Supabase Client   │
│ .from('...')      │
└─────────┬─────────┘
          │
          ▼
┌───────────────────┐
│ PostgreSQL        │
│ Query execution   │
└─────────┬─────────┘
          │
          ▼ (return data)
┌───────────────────┐
│ React Component   │
│ Render with data  │
└───────────────────┘
```

---

## 7. Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION ENVIRONMENT                   │
└─────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   GitHub     │
                    │  Repository  │
                    └──────┬───────┘
                           │
                           │ git push
                           ▼
                    ┌──────────────┐
                    │   GitHub     │
                    │   Actions    │
                    │   (CI/CD)    │
                    └──────┬───────┘
                           │
                           ├─► Run tests
                           ├─► Build Next.js
                           ├─► Run migrations
                           │
                           ▼
         ┌─────────────────┴─────────────────┐
         │                                   │
         ▼                                   ▼
┌─────────────────┐              ┌─────────────────┐
│     Vercel      │              │    Supabase     │
│   (Frontend)    │◄─────────────┤   (Backend)     │
│                 │   API calls  │                 │
│ • Next.js       │              │ • PostgreSQL    │
│ • Edge Network  │              │ • Auth          │
│ • Serverless    │              │ • Storage       │
└────────┬────────┘              │ • Realtime      │
         │                       └─────────────────┘
         │
         │ CDN
         ▼
┌─────────────────┐
│   CloudFlare    │
│   (Global CDN)  │
└────────┬────────┘
         │
         │ Fast delivery
         ▼
┌─────────────────┐
│     Users       │
│   (Worldwide)   │
└─────────────────┘


Monitoring:
───────────
┌─────────────────┐
│     Sentry      │  ◄─── Error tracking
└─────────────────┘

┌─────────────────┐
│    PostHog      │  ◄─── Product analytics
└─────────────────┘

┌─────────────────┐
│ Vercel Analytics│  ◄─── Performance monitoring
└─────────────────┘
```

---

## 8. Mobile App Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    MOBILE STRATEGY                          │
└─────────────────────────────────────────────────────────────┘

Phase 1: PWA (Progressive Web App)
───────────────────────────────────
┌─────────────────┐
│   Web Browser   │
│    (Safari/     │
│     Chrome)     │
└────────┬────────┘
         │
         │ Access web app
         ▼
┌─────────────────┐
│   Next.js PWA   │
│                 │
│ • Responsive    │
│ • Offline       │
│ • Install       │
│ • Push Notif    │
└─────────────────┘

Pros: Fast deployment, single codebase
Cons: Limited native features


Phase 2: Capacitor Wrapper
───────────────────────────
┌─────────────────────────────────────┐
│        Native App Shell             │
│        (Capacitor)                  │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐ │
│  │      WebView                  │ │
│  │      (Next.js PWA runs here)  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Native Plugins               │ │
│  │  • Camera                     │ │
│  │  • Push Notifications         │ │
│  │  • File System                │ │
│  │  • Geolocation                │ │
│  └───────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
         │                 │
         ▼                 ▼
┌──────────────┐  ┌──────────────┐
│  iOS Device  │  │Android Device│
└──────────────┘  └──────────────┘

Pros: Native app experience, access to native features
Cons: App store review, slightly more complex


Phase 3: React Native (Optional Future)
────────────────────────────────────────
Fully native apps with React Native
Pros: Best performance, full native access
Cons: Separate codebase, more maintenance
```

---

## 9. Real-time Features Flow

```
┌─────────────────────────────────────────────────────────────┐
│             REAL-TIME UPDATES (Supabase Realtime)           │
└─────────────────────────────────────────────────────────────┘

Scenario: New announcement posted

Admin creates announcement
        │
        ▼
┌───────────────────┐
│  API Route        │
│  POST /api/       │
│  announcements    │
└─────────┬─────────┘
          │
          │ INSERT
          ▼
┌───────────────────┐
│  PostgreSQL       │
│  announcements    │
│  table            │
└─────────┬─────────┘
          │
          │ Database trigger
          ▼
┌───────────────────┐
│  Supabase         │
│  Realtime         │
│  Server           │
└─────────┬─────────┘
          │
          │ WebSocket broadcast
          │
    ┌─────┴─────┬─────────┬─────────┐
    │           │         │         │
    ▼           ▼         ▼         ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ User 1 │ │ User 2 │ │ User 3 │ │ User N │
│ Browser│ │ Browser│ │ Browser│ │ Browser│
└────────┘ └────────┘ └────────┘ └────────┘
    │           │         │         │
    │           │         │         │
    └─────┬─────┴─────────┴─────────┘
          │
          │ Update UI automatically
          ▼
┌───────────────────┐
│  React Component  │
│  re-renders with  │
│  new data         │
└───────────────────┘


Client-side subscription:
─────────────────────────
const channel = supabase
  .channel('announcements')
  .on('postgres_changes', {
    event: 'INSERT',
    schema: 'public',
    table: 'announcements',
    filter: `church_id=eq.${churchId}`
  }, (payload) => {
    // Add new announcement to state
    setAnnouncements(prev => [payload.new, ...prev]);
  })
  .subscribe();
```

---

## 10. Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                    │
└─────────────────────────────────────────────────────────────┘

Layer 1: Network Security
─────────────────────────
┌─────────────────┐
│   CloudFlare    │
│   Firewall      │  ◄─── DDoS protection, rate limiting
└────────┬────────┘
         │
         │ HTTPS only
         ▼

Layer 2: Authentication
───────────────────────
┌─────────────────┐
│   Supabase      │
│   Auth          │  ◄─── JWT tokens, OAuth, MFA
└────────┬────────┘
         │
         │ Verify user
         ▼

Layer 3: Authorization (Row Level Security)
────────────────────────────────────────────
┌─────────────────┐
│  PostgreSQL     │
│  RLS Policies   │  ◄─── User can only access own church's data
└────────┬────────┘
         │
         │ Filter rows
         ▼

Layer 4: Input Validation
──────────────────────────
┌─────────────────┐
│   Zod Schema    │
│   Validation    │  ◄─── Validate all inputs
└────────┬────────┘
         │
         │ Sanitized data
         ▼

Layer 5: Business Logic
────────────────────────
┌─────────────────┐
│   API Route     │
│   Authorization │  ◄─── Check permissions (admin, leader, etc.)
└────────┬────────┘
         │
         │ Authorized action
         ▼

Layer 6: Database Security
───────────────────────────
┌─────────────────┐
│  PostgreSQL     │
│  Constraints    │  ◄─── Foreign keys, unique constraints
└────────┬────────┘
         │
         ▼
     Success!


Example RLS Policy:
───────────────────
CREATE POLICY "Users can view own church data"
ON announcements FOR SELECT
USING (
  church_id IN (
    SELECT church_id 
    FROM users 
    WHERE id = auth.uid()
  )
);

This ensures users can ONLY see announcements from their church!
```

---

## 11. Phase Implementation Timeline

```
┌─────────────────────────────────────────────────────────────┐
│                    24-WEEK TIMELINE                         │
└─────────────────────────────────────────────────────────────┘

Week 1-4: PHASE 1 - Foundation
═══════════════════════════════
├─ Week 1: Project setup, Supabase, Auth
├─ Week 2: User profiles, Dashboard
├─ Week 3: Bible reading migration
└─ Week 4: Reading plans, Testing
    ✅ Deliverable: Working auth + Bible reading with cloud sync

Week 5-8: PHASE 2 - Church Core
════════════════════════════════
├─ Week 5: Announcements system
├─ Week 6: Sermon library
├─ Week 7: Events calendar
└─ Week 8: Push notifications
    ✅ Deliverable: Church management features

Week 9-12: PHASE 3 - Community
═══════════════════════════════
├─ Week 9: Community posts
├─ Week 10: Kids corner
├─ Week 11: Prayer wall
└─ Week 12: Small groups
    ✅ Deliverable: Social/community features

Week 13-16: PHASE 4 - Engagement
═════════════════════════════════
├─ Week 13: Quiz system
├─ Week 14: Achievements
├─ Week 15: Enhanced Bible features
└─ Week 16: Devotionals
    ✅ Deliverable: Engagement & gamification

Week 17-20: PHASE 5 - Admin
════════════════════════════
├─ Week 17: Admin dashboard
├─ Week 18: Analytics
├─ Week 19: Advanced notifications
└─ Week 20: Church management
    ✅ Deliverable: Complete admin tools

Week 21-24: PHASE 6 - Mobile & Polish
══════════════════════════════════════
├─ Week 21: iOS/Android apps
├─ Week 22: Performance optimization
├─ Week 23: Testing & QA
└─ Week 24: Documentation & launch prep
    ✅ Deliverable: Production-ready platform


Milestone Checkpoints:
══════════════════════
┌───┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┬────┐
│ 4 │ 8  │ 12 │ 16 │ 20 │ 24 │ Weeks
└───┴────┴────┴────┴────┴────┘
  ▼    ▼    ▼    ▼    ▼    ▼
  ✓    ✓    ✓    ✓    ✓    ✓
 MVP  Core  Comm. Eng. Admin Launch
```

---

## Summary

These diagrams provide a visual understanding of:

1. **System Architecture**: How components interact
2. **Data Flow**: How data moves through the system
3. **Database Design**: Table relationships
4. **Authentication**: User login flow
5. **Module Structure**: How features are organized
6. **State Management**: Different state layers
7. **Deployment**: Production environment
8. **Mobile Strategy**: Progressive enhancement
9. **Real-time**: Live updates mechanism
10. **Security**: Multiple protection layers
11. **Timeline**: Project phases

Use these diagrams as reference while building the enterprise church app!

---

*For detailed code examples, see TECHNICAL_ARCHITECTURE.md*
*For feature specifications, see ENTERPRISE_DEVELOPMENT_PLAN.md*
*For getting started, see QUICK_START_GUIDE.md*
